/**
 * @interface FormattedPackageMetadata
 * Used for formatting the package metadata to be returned to the client.
 */

export interface FormattedPackageMetadata {
    Name: string;
    Version: string;
    ID: string;
}